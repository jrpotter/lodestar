clean up code
    Menu:
        DrawLinks(links, line)
        Active

    sorted links are what is cached.
    Create nodes immediately

    Spoof Nodes:
        spoof
        wiki
        category
            subcategories

change help last date
